# 서버 이전 가이드 (기존 서버 → 새 서버, Tailscale HTTPS)

이 문서는 투자기획팀 업무관리 공간 서버를 **새 PC로 이전**할 때 참고하는 절차서다.
새 서버에서 이 저장소를 clone 한 뒤 Kiro IDE(또는 담당자)가 이 문서를 기준으로
서버를 구축하고, 아래 "새 서버에서 확정해야 할 항목"을 채워 회신한다.

현재 릴리스: **1.5.1** (서버/클라이언트 공통 버전). 사용자는 1.4.1 → 1.5.1로 바로 업데이트한다
(1.5.0은 건너뜀). 자동 서버 전환 + 업데이트 안내/강제 기능이 1.5.1에 함께 포함된다.
전환 방식: **방식 A — 사용자 없는 시간(주말/야간)에 짧은 점검창(cutover)** 을 두고,
그 동안 데이터를 마이그레이션한 뒤 새 서버로 전환한다. (마이그레이션 이후 옛 서버에
데이터가 더 쌓이는 위험을 없애기 위함.)

---

## 1. 서버 구조 요약 (코드 기준)

- 런타임: **Node.js** (검증 환경 Node 24), 서버 진입점 `server/index.js`.
- DB: **better-sqlite3** 단일 파일. `apps/desktop/electron/db.js`가 스키마/쿼리를 소유하고
  서버가 이를 import 한다. WAL 모드(`todo_app.db` + `-wal` + `-shm`).
- HTTP + SSE. 기본 포트 **4310**, `0.0.0.0` 바인딩.
- 헬스체크: `GET /health` → `{ status, version }` (버전이 serverVersion과 일치해야 함).
- 자동 업데이트 피드: 서버가 `TODO_UPDATES_PATH`의 `latest.yml` + 설치 파일을
  `GET /updates/...` 로 서빙하고, 파일 변경 시 접속 중 클라이언트에 푸시한다.
  클라이언트는 `{자신이 로그인 시 입력한 서버주소}/updates` 를 피드로 본다.

### 데이터/상태 위치 (환경변수 → 기본값)
| 항목 | 환경변수 | 기본값 | 이전 필요 |
|------|----------|--------|:--------:|
| DB 파일 | `TODO_DATABASE_PATH` | `server/database/todo_app.db` (+ `-wal`, `-shm`) | ✅ 필수 |
| 첨부파일 | `TODO_UPLOADS_PATH` | `server/uploads/` | ✅ 필수 |
| 자동 업데이트 파일 | `TODO_UPDATES_PATH` | `server/updates/` (`latest.yml` + 설치 EXE) | ✅ 필수 |
| 백업 | `TODO_BACKUP_PATH` | `server/backups/` | 선택(스냅샷) |
| 기타 설정 | `server/.env` | `.env.example` 참고 | 값 재작성 |

> 위 데이터 폴더들은 `.gitignore`로 저장소에서 제외되어 있다. **git으로는 넘어오지 않으니
> cutover 시 파일 복사로 옮겨야 한다.**

### 실행 방법
- 현재(Windows): `server/start-server.ps1` 가 `.env`를 읽어 환경변수 세팅 후
  `node index.js` 실행. 자동 시작/백업은 `install-windows-tasks.ps1`.
- 새 서버가 다른 OS/컨테이너면 그에 맞는 실행 방식으로 대체하되, **위 환경변수와 포트,
  그리고 데이터 3종(DB/uploads/updates)의 영구 저장(볼륨)** 을 반드시 보장할 것.

---

## 2. 새 서버 구성 시 결정 사항 (새 서버 Kiro가 판단)

새 PC의 실제 환경을 확인한 뒤 아래를 결정한다.

1. **실행 형태**: 네이티브(Node 직접) vs **컨테이너(Docker 등)**.
   - 컨테이너면: `better-sqlite3`는 네이티브 모듈이라 **해당 아키텍처(x86_64/arm64)에서
     빌드/재빌드** 되어야 한다. 베이스 이미지의 glibc/musl(alpine 주의)도 확인.
   - **볼륨 필수**: DB/uploads/updates 3종을 호스트 볼륨에 마운트해 컨테이너 교체에도
     데이터가 유지되게 한다.
2. **Node 버전**: 검증 환경은 Node 24. 새 환경의 Node 버전을 확인하고 `npm ci` 후
   `npm run test:server`가 통과하는지 검증.
3. **Tailscale + HTTPS 연결 주소 확정**:
   - Tailscale로 포트를 열고 **HTTPS** 로 서비스한다. 최종 클라이언트 접속 주소(FQDN)를
     확정한다. 예: `https://<host>.<tailnet>.ts.net` (MagicDNS + Tailscale 인증서) 또는
     리버스 프록시(caddy 등)로 감싼 HTTPS 엔드포인트.
   - 서버 앱 자체는 HTTP(4310)만 제공하므로, **HTTPS 종단(TLS)은 Tailscale serve /
     리버스 프록시가 담당**하고 내부로 4310에 프록시하는 구성을 권장.
   - `/updates` 도 같은 HTTPS 엔드포인트로 노출되어야 자동 업데이트가 HTTPS로 동작.

### ⚠️ 자동 업데이트 HTTPS 검증 (중요)
클라이언트의 electron-updater가 **새 서버의 HTTPS 인증서를 신뢰**해야 자동 업데이트가
계속 동작한다. Tailscale/`*.ts.net` 인증서(공인 CA)는 대체로 신뢰되지만, **반드시
실측 검증**할 것:
- 새 HTTPS 주소로 `GET /health` 200 확인 (curl 등, 인증서 오류 없이).
- `GET /updates/latest.yml` 이 200으로 응답하는지 확인.
- 가능하면 테스트 클라이언트에서 새 주소로 로그인 → 자동 업데이트 확인까지.

---

## 3. 새 서버에서 확정해 회신할 항목 (자동 전환 코드에 필요)

아래를 확정해 알려주면, 클라이언트 **자동 전환 버전(1.5.0 예정)** 을 구현한다.
(자동 전환 = 앱이 저장해 둔 옛 서버 주소를 새 주소로 자동 교체 + 재로그인 유도)

- [ ] **새 서버 최종 접속 주소(HTTPS FQDN)**: 예) `https://xxx.ts.net`  ← 하드코딩 매핑의 목적지
- [ ] **옛 서버 주소(들)**: 사용자가 현재 앱에 입력해 쓰는 값 전부. 예) `http://192.168.0.10:4310`
      (자동 전환은 "저장된 주소가 옛 주소와 일치하면 새 주소로 교체" 방식이라 정확해야 함)
- [ ] **실행 형태**(네이티브/컨테이너)와 데이터 3종의 실제 경로(볼륨 마운트 지점)
- [ ] `GET /health` 및 `GET /updates/latest.yml` 이 **새 HTTPS 주소에서 200** 인지(인증서 OK)
- [ ] 새 서버 Node 버전 및 `npm run test:server` 통과 여부

---

## 4. 새 서버 구축 절차 (데이터는 아직 옮기지 않음 — 빈 서버로 검증)

1. 저장소 clone, `apps/desktop`에서 `npm ci`.
2. `server/.env` 작성(`.env.example` 기반). 경로/포트/`TODO_ADMIN_PASSWORD` 설정.
   - 이 단계는 **빈 DB**로 뜨므로 `TODO_ADMIN_PASSWORD`로 admin 계정이 생성된다.
     (실데이터는 cutover 때 덮어쓸 것이므로 임시.)
3. `npm run test:server` 로 서버 로직 검증(PASS 확인).
4. 서버 기동(`start-server.ps1` 또는 컨테이너) → 로컬 `GET /health` = **1.5.1** 확인.
5. Tailscale + HTTPS 종단 구성 → **외부에서 새 HTTPS 주소로 `/health` 200** 확인 →
   **최종 접속 주소 확정**.
6. 위 3장 체크리스트를 채워 회신.

---

## 5. Cutover(전환) 당일 절차 — 방식 A (사용자 없는 시간)

> 목표: 마이그레이션 이후 옛 서버에 새 데이터가 쌓이지 않도록, 짧은 점검창에 한 번에 전환.

1. **공지**: 점검 시간 안내(수 분~수십 분).
2. **옛 서버 쓰기 차단**: 옛 서버 프로세스를 중지(또는 접속 차단). 이때부터 아무도
   옛 서버에 데이터를 쓰지 못한다.
3. **옛 서버 백업**: `backup-server.ps1`(중지 전이라면) 또는 파일 직접 복사.
   복사 대상: `database/todo_app.db`(+`-wal`,`-shm`), `uploads/`, `updates/`.
   - WAL 때문에 **서버 중지 후 복사**가 가장 안전.
4. **새 서버로 데이터 복원**: 위 3종을 새 서버의 대응 경로(볼륨)로 복사. 새 서버가
   빈 DB로 만든 임시 admin은 실데이터로 덮인다(기존 admin 비밀번호 유지됨).
5. **새 서버 기동·검증**: `/health` 1.5.1, 새 HTTPS 주소로 로그인, 데이터/첨부/계정 정상 확인.
6. **자동 전환 버전(1.5.1) 배포**:
   - 새 서버와 (전환 기간 유지할) 옛 서버 **양쪽의 `/updates`** 에 동일한 1.5.1 설치본 배치.
   - 사용자가 앱을 켜면 업데이트 → 새 버전이 서버 주소를 새 HTTPS로 자동 교체 + 재로그인.
7. **옛 서버 유지**: 아직 업데이트를 못 받은 사용자를 위해 옛 서버의 `/updates`는
   전환 기간 동안 유지(단, 앱 접속/쓰기는 차단 상태). 전원 전환 확인 후 옛 서버 정리.

---

## 6. 주의
- 소스 인코딩 UTF-8(BOM 없음) 유지. 빌드 산출물/데이터 폴더는 커밋하지 않는다.
- 스키마 변경은 없다(기존 DB 그대로 복원 가능). 마이그레이션 오류가 나면 중단하고 로그 확인.
- 데이터 복사는 반드시 **옛 서버 중지 상태**에서.

# 서버 이전 진행 현황 (옛 서버 PC 회신)

이 문서는 SERVER-MIGRATION.md의 짝 문서로, **옛 서버 PC(현재 운영/배포 중인 PC)**의
현황과 대기 상태, 그리고 새 서버 측/조율자(Kiro Cloud)에게 묻는 사항을 정리한다.
3자(옛 서버 PC / 새 서버 PC / Kiro Cloud)가 GitHub를 통해 이 문서로 진행 상황을 공유한다.

최종 업데이트: 옛 서버 PC 점검 기준.

---

## A. 옛 서버 PC 현황 (점검 완료)

- 서버 상태: `GET /health` = **1.4.1** 정상 동작 중.
- 코드 상태: 로컬 HEAD = origin/main, tracked 변경 없음(clean).
- 이 PC의 접속 주소 후보:
  - 사내망(이더넷): `http://130.1.14.61:4310`
  - Tailscale: `http://100.65.76.14:4310`

### cutover 때 옮길 데이터 (git 제외 대상 → 파일 복사 필요)
| 항목 | 경로 | 크기(현재) |
|------|------|-----------|
| DB (3파일 세트) | server/database/todo_app.db (+ -wal, -shm) | 약 10 MB |
| 첨부파일 | server/uploads/ | 약 1.2 MB |
| 자동 업데이트 | server/updates/ (latest.yml + 설치 EXE 다수) | 약 1.74 GB |
| (선택) 백업 | server/backups/ | 약 32 MB |

### 데이터 이전 수단
- **OneDrive 공유 폴더**를 통해 데이터 3종을 옮긴다.
- 주의: DB는 WAL 모드이므로 **옛 서버 중지 후** todo_app.db / -wal / -shm 3파일을 함께 복사.

---

## B. 현재 대기 상태

옛 서버 PC는 **cutover 실행 대기 중**이다. 아래 선행 조건이 충족되면 진행한다.
- 새 서버 구축·검증 완료 및 SERVER-MIGRATION.md 3장 체크리스트 회신.
- 특히 "새 서버 최종 HTTPS 주소"와 "옛 서버 주소 매핑"이 확정되어야
  클라이언트 자동 전환 버전(1.5.0)을 만들 수 있음.

---

## C. 확인/질문 사항

### C-1. 새 서버 PC(구축 담당 Kiro)에게
1. 새 서버 최종 접속 주소(HTTPS FQDN) 확정본은?
2. `GET /health` = 1.4.1 및 `GET /updates/latest.yml` 이 새 HTTPS 주소에서
   인증서 오류 없이 200 인가?
3. 실행 형태(네이티브 Node / 컨테이너)와 데이터 3종의 실제 저장 경로(볼륨)?
4. 새 서버 Node 버전과 `npm run test:server` 통과 여부?
5. OneDrive로 받을 데이터의 배치 경로는? (옛 서버가 OneDrive에 올릴 폴더 구조 합의 필요)

### C-2. Kiro Cloud(앱 개발/조율)에게
1. 클라이언트 자동 전환 버전은 1.5.0으로 진행하는가?
   - 동작: 앱에 저장된 옛 서버 주소가 지정된 값과 일치하면 새 HTTPS 주소로 자동 교체 + 재로그인 유도.
2. 자동 전환 매핑에 넣을 "옛 주소"는 위 A의 두 주소(130.1.14.61:4310 / 100.65.76.14:4310)
   중 실제 사용자가 입력해 쓰는 값 기준으로 확정 필요. (사용자 확인 항목)
3. 전환 기간 동안 옛/새 서버 양쪽 /updates 에 1.5.0 설치본을 함께 배치하는 절차 확정?

### C-3. 사용자 확인 필요 (옛 서버 PC 담당이 확인)
- 실제 사용자들이 앱 로그인 시 입력해 쓰는 옛 서버 주소가 정확히 무엇인지 확정.
  (자동 전환 매핑의 출발점이라 정확해야 함)

---

## D. 다음 작업 주체

- **다음 차례: 새 서버 PC(구축 담당 Kiro)** — SERVER-MIGRATION.md 4장 절차로 빈 서버 구축·검증 후
  3장 체크리스트를 채워 이 문서 C-1에 회신.
- 옛 서버 PC: 회신 수신 및 사용자 옛 주소 확정까지 대기. 이후 cutover(OneDrive 데이터 이전 + 전환) 실행.
- Kiro Cloud: 1.5.0 자동 전환 버전 설계는 새 HTTPS 주소 확정 후 착수.